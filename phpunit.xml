<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
    backupGlobals="false"
    backupStaticProperties="false"
    bootstrap="vendor/autoload.php"
    cacheDirectory="storage/framework/cache/phpunit"
    colors="true"
    displayDetailsOnTestsThatTriggerErrors="true"
    displayDetailsOnTestsThatTriggerNotices="true"
    displayDetailsOnTestsThatTriggerWarnings="true"
    displayDetailsOnPhpunitDeprecations="true"
    processIsolation="false"
    stopOnFailure="false"
    stopOnError="false"
    beStrictAboutTestsThatDoNotTestAnything="false">
    <source>
        <include>
            <directory suffix=".php">app</directory>
        </include>
        <exclude>
            <directory suffix=".php">app/Protobuf</directory>
        </exclude>
    </source>
    <!--
        The <coverage> element only declares WHAT to measure (the app/ source
        tree above); it never produces a report unless a coverage driver
        (Xdebug/PCOV) is present AND a <report> is requested. To keep
        `composer test` green on runners without a coverage driver, NO
        <report> is hardcoded here — reports are requested via CLI flags in
        the CI coverage job and the test-coverage-html composer script.
        See .github/workflows/ci.yml.
    -->
    <coverage>
    </coverage>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">tests/Feature</directory>
        </testsuite>
        <testsuite name="Api.v1">
            <directory suffix="Test.php">tests/Api/v1</directory>
        </testsuite>
    </testsuites>
    <php>
        <ini name="memory_limit" value="2048M"/>
        <env name="APP_ENV" value="testing"/>
        <!-- following values override .env.testing vars -->
    </php>
</phpunit>
